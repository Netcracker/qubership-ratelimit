# Adds namespace to all resources.
namespace: ratelimit-system

# Value of this field is prepended to the names of all resources, e.g. a
# Deployment named "controller-manager" becomes "ratelimit-controller-manager".
namePrefix: ratelimit-

resources:
  - ../crd
  - ../rbac
  - ../manager

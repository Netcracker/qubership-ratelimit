# More info: https://docs.docker.com/engine/reference/builder/#dockerignore-file
# Ignore everything by default and re-include only needed files
**

# Re-include Go source files (but not *_test.go)
!**/*.go
**/*_test.go

# Re-include Go module files, the nested engine module included
!**/go.mod
!**/go.sum

# The Redis store embeds its counting script. Nothing referenced that package
# until the operator selected the shared store, so the missing file surfaced as a
# build failure rather than as a missing feature.
!engine/store/redis/*.lua

# configloader's YAML property source reads this next to the binary.
!application.yaml

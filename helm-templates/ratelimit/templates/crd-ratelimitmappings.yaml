# Generated by "make sync-helm-crds" from config/crd/bases. DO NOT EDIT.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    # The CRD is cluster-scoped and shared by every namespace
    # installation, so uninstalling one release must not remove it.
    helm.sh/resource-policy: keep
    controller-gen.kubebuilder.io/version: v0.21.0
  name: ratelimitmappings.ratelimit.netcracker.com
spec:
  group: ratelimit.netcracker.com
  names:
    categories:
    - ratelimit
    kind: RateLimitMapping
    listKind: RateLimitMappingList
    plural: ratelimitmappings
    shortNames:
    - rlm
    singular: ratelimitmapping
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.activeGeneration
      name: Active
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RateLimitMapping is the singleton that tells one domain how to read identity
          out of a JWT, and holds the client groups its policies share.

          The singleton comes out of the naming rule rather than out of arbitration:
          metadata.name equals spec.domain, and object names are unique in a namespace,
          so a second mapping for a domain cannot be created. The API server rejects it
          with AlreadyExists, and there is no "which one wins" question to answer.

          A policy does not wait for a mapping, but it does not run half-way either. A
          policy over built-in keys alone works with no mapping present; a policy
          referencing declared keys or shared groups is invalid as a whole until the
          mapping appears, and comes alive on the same domain rebuild.

          Updating a mapping goes through a transaction gate that vetoes a candidate
          which would stop rules that are running. Deleting one does not: that is a
          deliberate administrative act, the domain falls back to the built-in keys, and
          the policies depending on it lose validity. RBAC is the guard against deleting
          it by accident, not the controller.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              RateLimitMappingSpec declares how the domain reads identity out of a token,
              and the client groups its policies share.
            properties:
              domain:
                description: |-
                  Domain is the traffic source this mapping serves. It equals
                  metadata.name, which is what makes the mapping a singleton.
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([a-z0-9.-]*[a-z0-9])?$
                type: string
              groups:
                description: |-
                  Groups are the client lists shared by every policy of the domain. A
                  policy that defines a group of the same name shadows the shared one.
                items:
                  description: |-
                    ClientGroup names a list of client identities that the InGroup operator
                    matches against.

                    A group defined by a policy is private to that policy; a group defined by the
                    mapping of the domain is visible to every policy of the domain. A private
                    name shadows a shared one.
                  properties:
                    clients:
                      description: |-
                        Clients lists the members of the group. Values are compared after the
                        same lower-case normalization the client key goes through, so the case a
                        token happens to carry does not decide membership.
                      items:
                        maxLength: 253
                        type: string
                      maxItems: 1024
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                    name:
                      description: |-
                        Name is how a predicate references the group. It is unique among the
                        groups of its object.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([a-z0-9._-]*[a-z0-9])?$
                      type: string
                  required:
                  - clients
                  - name
                  type: object
                maxItems: 64
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              mappings:
                description: |-
                  Mappings are the declared keys of the domain. The built-in client key
                  works with no mapping present; an entry named client overrides it.
                items:
                  description: ClaimMapping turns one JWT claim into one descriptor
                    key.
                  properties:
                    claim:
                      description: Claim is a dotted path into the payload, such as
                        realm_access.roles.
                      maxLength: 253
                      minLength: 1
                      type: string
                    claimPath:
                      description: |-
                        ClaimPath is the same path given segment by segment, which is how a claim
                        name that itself contains a dot is addressed.
                      items:
                        maxLength: 253
                        minLength: 1
                        type: string
                      maxItems: 8
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                    fallbacks:
                      description: |-
                        Fallbacks are dotted paths tried in order when the primary path yields
                        nothing. The first non-empty result wins.
                      items:
                        maxLength: 253
                        minLength: 1
                        type: string
                      maxItems: 3
                      type: array
                      x-kubernetes-list-type: atomic
                    key:
                      description: Key names the descriptor key the rules of the domain
                        reference.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z][a-z0-9_]*$
                      type: string
                    normalize:
                      default: None
                      description: Normalize is applied to the extracted value.
                      enum:
                      - None
                      - Lowercase
                      type: string
                    type:
                      default: String
                      description: Type is the shape of the extracted value.
                      enum:
                      - String
                      - StringArray
                      type: string
                  required:
                  - key
                  type: object
                  x-kubernetes-validations:
                  - message: a mapping takes exactly one of claim and claimPath
                    rule: has(self.claim) != has(self.claimPath)
                  - message: path, method and token are produced by the engine and
                      cannot be redefined
                    rule: '!(self.key in [''path'', ''method'', ''token''])'
                maxItems: 32
                type: array
                x-kubernetes-list-map-keys:
                - key
                x-kubernetes-list-type: map
            required:
            - domain
            type: object
          status:
            description: RateLimitMappingStatus is the observed state of a RateLimitMapping.
            properties:
              activeGeneration:
                description: |-
                  ActiveGeneration is the generation actually in effect. It falls behind
                  ObservedGeneration when the latest edit is not the one being enforced and an
                  earlier, last-good generation keeps running. Zero means nothing of this
                  object is in effect at all.
                format: int64
                type: integer
              conditions:
                description: Conditions holds the latest observations of the mapping
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              effectiveKeys:
                description: |-
                  EffectiveKeys is the set of keys the domain produces: the built-in keys
                  plus the mapped ones. It reports the active generation rather than the
                  candidate, so a rule author reads what is in effect rather than what was
                  asked for. Route captures are per block and do not appear here.
                items:
                  type: string
                maxItems: 64
                type: array
                x-kubernetes-list-type: atomic
              observedGeneration:
                description: ObservedGeneration is the latest spec generation the
                  operator has seen.
                format: int64
                type: integer
              rejectedBy:
                description: RejectedBy lists the policies that vetoed the latest
                  generation.
                items:
                  description: |-
                    MappingRejection names one policy that vetoed a mapping generation.

                    Generation is the generation that was vetoing, which is the one that was
                    running — not necessarily the latest in etcd. Without it the culprit cannot be
                    found, because that spec may no longer exist as written.
                  properties:
                    block:
                      description: Block and Rule locate the reference that the candidate
                        would break.
                      type: string
                    generation:
                      description: Generation is the generation of the policy that
                        was running.
                      format: int64
                      type: integer
                    policy:
                      description: Policy is the name of the vetoing policy.
                      type: string
                    reason:
                      description: Reason is one of the Problem constants of this
                        package.
                      type: string
                    rule:
                      type: string
                  required:
                  - generation
                  - policy
                  - reason
                  type: object
                maxItems: 64
                type: array
                x-kubernetes-list-type: atomic
            type: object
        type: object
        x-kubernetes-validations:
        - message: 'metadata.name has to equal spec.domain: the mapping is the singleton
            of its domain'
          rule: self.metadata.name == self.spec.domain
    served: true
    storage: true
    subresources:
      status: {}

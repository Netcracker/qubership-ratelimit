# Generated by "make sync-helm-crds" from config/crd/bases. DO NOT EDIT.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    # The CRD is cluster-scoped and shared by every namespace
    # installation, so uninstalling one release must not remove it.
    helm.sh/resource-policy: keep
    controller-gen.kubebuilder.io/version: v0.21.0
  name: ratelimitpolicies.ratelimit.netcracker.com
spec:
  group: ratelimit.netcracker.com
  names:
    categories:
    - ratelimit
    kind: RateLimitPolicy
    listKind: RateLimitPolicyList
    plural: ratelimitpolicies
    shortNames:
    - rlp
    singular: ratelimitpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.problems
      name: Problems
      type: integer
    - jsonPath: .status.activeGeneration
      name: Active
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RateLimitPolicy is a set of rate limit rules bound to one gateway domain.

          Policies are units of ownership and review rather than units of evaluation:
          every event recompiles the whole domain, and after compilation there are no
          policies left, only one flat set of blocks.

          A generation is enforced whole or not at all. That makes the layout of a
          namespace a design choice: keep the safety-net total limits in a small policy
          of their own, over built-in keys, where they can hardly ever become invalid.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RateLimitPolicySpec is a set of rate limit rules bound to
              one domain.
            properties:
              domain:
                description: |-
                  Domain binds this policy to a traffic source. It has to equal, byte for
                  byte, the domain the rate limit filter of that gateway sends. Comparison
                  is case-sensitive.

                  A colon separates the segments of a counter key, and braces are Redis
                  Cluster hash tags that decide slot routing, so neither is allowed here.
                  The naming convention is <kind>.<name>: gateway.public, service.billing.
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([a-z0-9.-]*[a-z0-9])?$
                type: string
              groups:
                description: |-
                  Groups are the client lists private to this policy. A name defined here
                  shadows a group of the same name in the mapping of the domain.
                items:
                  description: |-
                    ClientGroup names a list of client identities that the InGroup operator
                    matches against.

                    A group defined by a policy is private to that policy; a group defined by the
                    mapping of the domain is visible to every policy of the domain. A private
                    name shadows a shared one.
                  properties:
                    clients:
                      description: |-
                        Clients lists the members of the group. Values are compared after the
                        same lower-case normalization the client key goes through, so the case a
                        token happens to carry does not decide membership.
                      items:
                        maxLength: 253
                        type: string
                      maxItems: 1024
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                    name:
                      description: |-
                        Name is how a predicate references the group. It is unique among the
                        groups of its object.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([a-z0-9._-]*[a-z0-9])?$
                      type: string
                  required:
                  - clients
                  - name
                  type: object
                maxItems: 64
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              limits:
                description: Limits are the blocks of the policy.
                items:
                  description: |-
                    LimitBlock is a target plus the rules that count the traffic it selects.
                    Blocks always add up: a request that lands in several blocks has to fit the
                    verdict of each.
                  properties:
                    mode:
                      default: All
                      description: |-
                        Mode selects how the rules of the block combine. It has no effect across
                        blocks.
                      enum:
                      - All
                      - FirstMatch
                      type: string
                    name:
                      description: Name is unique within its policy and is part of
                        the counter key.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([a-z0-9._-]*[a-z0-9])?$
                      type: string
                    rules:
                      description: Rules are the counters of the block.
                      items:
                        description: Rule is one counter of a block.
                        properties:
                          behavior:
                            default: Enforce
                            description: Behavior selects what the rule does with
                              the verdict.
                            enum:
                            - Enforce
                            - Shadow
                            - Bypass
                            type: string
                          counters:
                            description: |-
                              Counters are the axes of the bucket. An empty list gives the rule a
                              single shared bucket. A rule whose axis the request does not carry, such
                              as client for an anonymous caller, does not match: there is nothing to
                              key the bucket by.
                            items:
                              maxLength: 63
                              pattern: ^[a-z][a-z0-9_]*$
                              type: string
                            maxItems: 4
                            type: array
                            x-kubernetes-list-type: atomic
                          name:
                            description: Name is unique within its block and is part
                              of the counter key.
                            maxLength: 63
                            minLength: 1
                            pattern: ^[a-z0-9]([a-z0-9._-]*[a-z0-9])?$
                            type: string
                          rates:
                            description: Rates are the counting windows of the rule.
                            items:
                              description: |-
                                Rate is one counting window of a rule. Windows of a rule are independent
                                buckets, so a rate limit and a quota live side by side.
                              properties:
                                algorithm:
                                  default: GCRA
                                  description: Algorithm is a property of the window
                                    rather than of the rule.
                                  enum:
                                  - GCRA
                                  - FixedWindow
                                  type: string
                                burst:
                                  description: |-
                                    Burst is the bucket depth of a GCRA window. It defaults to Requests,
                                    which is a full bucket.
                                  format: int32
                                  minimum: 1
                                  type: integer
                                period:
                                  description: 'Period is the length of the window,
                                    written as one unit: 30s, 5m, 1h, 1d.'
                                  maxLength: 8
                                  pattern: ^[1-9][0-9]*(s|m|h|d)$
                                  type: string
                                requests:
                                  description: Requests is the quota of the window.
                                  format: int32
                                  minimum: 1
                                  type: integer
                              required:
                              - period
                              - requests
                              type: object
                              x-kubernetes-validations:
                              - message: burst is a GCRA parameter; FixedWindow entries
                                  reject it
                                rule: '!has(self.burst) || self.algorithm == ''GCRA'''
                              - message: period ranges from 1s to 1d
                                rule: self.period == '1d' || (duration(self.period)
                                  >= duration('1s') && duration(self.period) <= duration('24h'))
                            maxItems: 4
                            type: array
                            x-kubernetes-list-type: atomic
                          replaces:
                            description: |-
                              Replaces silences rules of the same block, which is how a narrow rule
                              overrides a broad one. It is available only in an All block, where the
                              order of the list carries no meaning of its own.
                            items:
                              maxLength: 63
                              type: string
                            maxItems: 16
                            type: array
                            x-kubernetes-list-type: atomic
                          when:
                            description: |-
                              When is a conjunction of predicates. An empty list matches every request
                              the block sees.
                            items:
                              description: |-
                                Predicate is one condition on the identity of the caller. Paths and methods
                                belong in the target of the block, so a predicate cannot name them.
                              properties:
                                key:
                                  description: Key names the descriptor key the predicate
                                    reads.
                                  maxLength: 63
                                  minLength: 1
                                  pattern: ^[a-z][a-z0-9_]*$
                                  type: string
                                operator:
                                  description: Operator is the predicate applied to
                                    the value set of the key.
                                  enum:
                                  - Equals
                                  - In
                                  - InGroup
                                  - Contains
                                  - Exists
                                  - NotExists
                                  type: string
                                value:
                                  description: |-
                                    Value is the operand of Equals, Contains, and InGroup. For InGroup it is
                                    the name of a group.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                values:
                                  description: Values is the operand of In.
                                  items:
                                    maxLength: 253
                                    type: string
                                  maxItems: 64
                                  minItems: 1
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                              x-kubernetes-validations:
                              - message: path and method are selected by target.routes,
                                  and token is an input rather than a key; none of
                                  them can appear in when
                                rule: '!(self.key in [''path'', ''method'', ''token''])'
                              - message: operators Equals, InGroup and Contains take
                                  value and reject values
                                rule: '!(self.operator in [''Equals'', ''InGroup'',
                                  ''Contains'']) || (has(self.value) && !has(self.values))'
                              - message: operator In takes values and rejects value
                                rule: self.operator != 'In' || (has(self.values) &&
                                  !has(self.value))
                              - message: operators Exists and NotExists take neither
                                  value nor values
                                rule: '!(self.operator in [''Exists'', ''NotExists''])
                                  || (!has(self.value) && !has(self.values))'
                            maxItems: 8
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - name
                        type: object
                        x-kubernetes-validations:
                        - message: a rule with behavior Bypass carries no rates; every
                            other rule carries at least one
                          rule: 'self.behavior == ''Bypass'' ? !has(self.rates) :
                            (has(self.rates) && size(self.rates) > 0)'
                        - message: the periods of a rule are unique
                          rule: '!has(self.rates) || self.rates.all(r, self.rates.filter(other,
                            other.period == r.period).size() == 1)'
                      maxItems: 128
                      minItems: 1
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    target:
                      description: |-
                        Target restricts the block. An absent target lets the block see the whole
                        domain.
                      properties:
                        routes:
                          description: Routes is an OR-list. A block without a target
                            sees the whole domain.
                          items:
                            description: |-
                              Route selects request traffic for a block. The fields of one route combine
                              with AND; the routes of a target combine with OR.
                            properties:
                              methods:
                                description: |-
                                  Methods accepts a request whose method is one of the listed values. An
                                  absent list accepts any method.
                                items:
                                  description: HTTPMethod is one request method a
                                    route accepts.
                                  enum:
                                  - GET
                                  - HEAD
                                  - POST
                                  - PUT
                                  - PATCH
                                  - DELETE
                                  - CONNECT
                                  - OPTIONS
                                  - TRACE
                                  type: string
                                maxItems: 9
                                type: array
                                x-kubernetes-list-type: atomic
                              path:
                                description: Path selects request paths.
                                properties:
                                  type:
                                    description: Type selects how Value is compared.
                                    enum:
                                    - Exact
                                    - Prefix
                                    - Template
                                    type: string
                                  value:
                                    description: |-
                                      Value is the path, the prefix, or the template. It starts with a slash;
                                      the query string of a request is cut before matching, so it never appears
                                      here.
                                    maxLength: 256
                                    minLength: 1
                                    pattern: ^/
                                    type: string
                                required:
                                - type
                                - value
                                type: object
                                x-kubernetes-validations:
                                - message: in a Template every segment is either a
                                    literal without braces or one whole placeholder
                                    {name}, where name matches ^[a-z][a-z0-9_]*$
                                  rule: self.type != 'Template' || self.value.matches('^(/([^/{}]+|[{][a-z][a-z0-9_]*[}]))+/?$')
                                - message: 'a Template placeholder cannot take the
                                    name of a built-in key: path, method, token, client'
                                  rule: self.type != 'Template' || !(self.value.contains('{path}')
                                    || self.value.contains('{method}') || self.value.contains('{token}')
                                    || self.value.contains('{client}'))
                            required:
                            - path
                            type: object
                            x-kubernetes-validations:
                            - message: the methods of a route are unique
                              rule: '!has(self.methods) || self.methods.all(m, self.methods.filter(x,
                                x == m).size() == 1)'
                          maxItems: 16
                          minItems: 1
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - routes
                      type: object
                  required:
                  - name
                  - rules
                  type: object
                  x-kubernetes-validations:
                  - message: replaces needs mode All; in a FirstMatch block the order
                      of the rules already decides
                    rule: self.mode != 'FirstMatch' || self.rules.all(r, !has(r.replaces))
                  - message: a Bypass rule in an All block names the rules it exempts
                      from in replaces; without them it is a silent no-op
                    rule: self.mode == 'FirstMatch' || self.rules.all(r, r.behavior
                      != 'Bypass' || (has(r.replaces) && size(r.replaces) > 0))
                maxItems: 64
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            required:
            - domain
            - limits
            type: object
          status:
            description: RateLimitPolicyStatus is the observed state of a RateLimitPolicy.
            properties:
              activeGeneration:
                description: |-
                  ActiveGeneration is the generation actually in effect. It falls behind
                  ObservedGeneration when the latest edit is not the one being enforced and an
                  earlier, last-good generation keeps running. Zero means nothing of this
                  object is in effect at all.
                format: int64
                type: integer
              conditions:
                description: Conditions holds the latest observations of the policy
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the latest spec generation the
                  operator has seen.
                format: int64
                type: integer
              problems:
                description: |-
                  Problems is the length of RuleProblems, which a printer column can show
                  and a JSONPath expression cannot compute.
                format: int32
                type: integer
              ruleProblems:
                description: |-
                  RuleProblems lists the diagnostics of the latest generation. A blocking
                  entry invalidates that generation whole: Ready goes false with a reason,
                  and an earlier generation keeps running where there is one. An
                  informational entry, such as CaptureShadowsMappedKey, leaves Ready alone —
                  a fact to alert on rather than a failure of the object.
                items:
                  description: |-
                    RuleProblem describes what is wrong with one rule. Problems live outside
                    conditions because conditions are a map keyed by type, and a policy can hold
                    several problems at once.

                    The list carries root causes only, never the cascade of every rule in an
                    invalid policy: the author needs the reference that does not resolve, not a
                    restatement of the atomicity rule once per rule.
                  properties:
                    block:
                      description: Block names the block the rule belongs to.
                      type: string
                    message:
                      description: Message says what the rule references and what
                        the domain offers.
                      type: string
                    reason:
                      description: Reason is one of the Problem constants of this
                        package.
                      type: string
                    rule:
                      description: Rule names the rule.
                      type: string
                  required:
                  - block
                  - reason
                  - rule
                  type: object
                maxItems: 256
                type: array
                x-kubernetes-list-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
